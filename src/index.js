import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//Below.. App is the component that we are rendering in the DOM in a div which will have id 'root'
ReactDOM.render(<App />, document.getElementById('root'));

